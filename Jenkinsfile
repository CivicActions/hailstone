pipeline {
    agent any


    
    environment {
                SSH_CREDS = credentials('halistone-aws-creds')
            }

    stages {
        stage('Build Image') {
            steps {
                    sh 'env'

            }
        }
    }
}
