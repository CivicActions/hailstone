pipeline {
    agent {
        docker { image 'hashicorp/packer' }
    }

    stages {
        stage('Build Image') {
            steps {
                    sh 'env'

            }
        }
    }
}
